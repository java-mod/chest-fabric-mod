# 공통 속성 — 버전별 값은 각 서브프로젝트의 gradle.properties에서 정의합니다.

# Fabric Loader (모든 버전 동일)
loader_version=0.18.6

# Mod 정보
mod_version=1.0.0
maven_group=com.example

# Gradle JVM
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
